const numbers: number[] = [1, 2, 3, 4, 5];

const names: Array<string> = ["emtiaz", "ahmed", "efty"];
console.log(numbers);
console.log(names);

// const items: string[] = [];
// items.push("keyboard");

// console.log(items);

const items = new Array<string>();
items.push("keyboard");

console.log(items);
